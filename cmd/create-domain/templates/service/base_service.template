package {domain_snake_name}_service

import (
	{domain_snake_name}_interfaces "{module_name}/internal/domains/{domain_snake_name}/interfaces"
)

type serviceImpl struct {
	PostgresRepository {domain_snake_name}_interfaces.PostgresRepository
}

func NewService(postgresRepository {domain_snake_name}_interfaces.PostgresRepository) {domain_snake_name}_interfaces.Service {
	return &serviceImpl{
        PostgresRepository: postgresRepository,
    }
}
