package {package_name}

import (
	"gorm.io/gorm"
)

type repositoryImpl struct {
	gorm *gorm.DB
}

func NewRepository(gorm *gorm.DB) Repository {
	return &repositoryImpl{gorm: gorm}
}

func (r *repositoryImpl) {pascal_name}() (err error) {
	return nil
}
